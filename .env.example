# Optional. With a key present, /api/chat answers with a language model; without
# one it answers from the local retrieval engine and the interface says so.
ANTHROPIC_API_KEY=

# Optional model override. Defaults to claude-opus-5.
COSMOS_MODEL=claude-opus-5
